1. Open "github.com"
2. sign in
3. clicked on New Repository button, and created a new repository called "basci_repo"
4. created a folder in my local repo called basic_repo
5. accessed local repo (basic_repo) via Git Bash
6. git init
7. created a txt file called some_files
8. then did these following commands in git bash:
	git add .
	git commit -m "some_files.txt"
	git remote add origin https://github.com/jerrycomortiz457/basic_repo.git
	git push -u origin master
9. made some changes on "some_files.txt"
	git add .
	git commit -m "some_files changes"
	git push -u origin master
10.git status
   git log
